import {StyleSheet} from 'react-native';
import AppStyles from 'common/AppStyles';
export default StyleSheet.create({
  container: {flex: 1, ...AppStyles.centerStyle},
  logoApp: {width: '50%', resizeMode: 'contain'},
});
